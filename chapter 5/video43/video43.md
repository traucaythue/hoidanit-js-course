## Truy cập, thêm, xóa thuộc tính của object

# Truy cập:

- Sử dụng var_name.property_name để lấy thuộc tính trong biến object

# Thêm thuộc tính vào object

- "var_name"."property_name" = "Giá trị cần thêm vào object"

# Xóa thuộc tính khỏi object

- delete "var_name"."property_name"
